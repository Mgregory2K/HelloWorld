name: GGG Award Research Pull

on:
  push:
    paths:
      - '.github/workflows/ggg-award-research.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pull-and-grade:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Pull and grade 1,000 likely-completed contract awards
        shell: bash
        run: |
          python